/*
import React from 'react'

function ActionButton({ icon, action }: { icon: React.ReactNode, action: (e: React.MouseEvent<HTMLButtonElement>) => void }) {
    return (
        <button
            className="bg-background cursor-pointer rounded-4xl border px-2 hover:border-secondary hover:text-secondary hover:bg-accent text-3xl"
            onClick={(e) => action(e)}>
            {icon}
        </button>
    )
}

export default ActionButton
*/